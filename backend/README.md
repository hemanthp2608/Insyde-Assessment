# Insyd Notify — Backend (Node + Express + SQLite)

## Quick Start
```bash
cd backend
npm install
node server.js
# API runs on http://localhost:4000
```

## Env (optional)
- `PORT` (default 4000)

## Notes
- Uses SQLite file `data.db` in this folder.
- Creates schema automatically if not found.
